# 🛠️ JWKL Cuisine 系統維護指南

本文件旨在提供系統管理員與開發者關於「小灶私廚」管理系統的維運指引，確保系統長期穩定運行。

---

## 🔐 安全性與 Token 管理

### 1. 輪替管理員密鑰 (ADMIN_TOKEN)
為了安全起見，建議每 3-6 個月更換一次 `ADMIN_TOKEN`。
1. 開啟 **Google Apps Script** 編輯器。
2. 點選左側齒輪 ⚙️ (專案設定)。
3. 在「指令碼屬性」中找到 `ADMIN_TOKEN`。
4. 修改值並儲存。
5. **重要**：修改後，請同步告知使用者在瀏覽器登入時更新密鑰。

### 2. 更新 LINE 通知 Token
若 LINE 官方帳號 Token 到期或失效：
1. 在專案設定的指令碼屬性中更新 `LINE_ACCESS_TOKEN`。
2. 若要更改接收溫知的管理員身分，請更新 `LINE_USER_ID`。

---

## 📊 試算表資料結構規範

### 1. 欄位擴充原則
若您需要在試算表（如 `訂單主檔`）中增加新欄位：
*   **建議做法**：將新欄位加在**最右側**。
*   **欄位名稱**：後端程式碼會自動透過標題偵測欄位，因此請確保標題名稱中包含關鍵字（如「電話」、「狀態」）。
*   **注意**：請勿刪除現有的 ID (第一欄) 或單號 (第二欄)，這會導致系統對位異常。

### 2. 關於「系統日誌」
當後端發生嚴重錯誤時，系統會自動在 Google Sheets 建立「系統日誌」分頁。
*   **狀態：嚴重**：代表後端拋出了 Exception，請檢查錯誤內容。
*   **定期清理**：若日誌過多（超過 1000 列），建議手動刪除舊紀錄以維持試算表效能。

---

## 💻 開發與除錯

### 1. 使用型別提示 (Typings)
專案中包含 `js/types.d.ts`。當您使用支援 TypeScript 的編輯器（如 Cursor / VSCode）修改 `js/` 下的檔案時：
*   將游標移至 `order` 或 `menuItem` 等變數上，可直接查看欄位屬性。
*   這能防止因拼錯欄位名稱（如 `訂單金額` vs `單價`）導致的 Bug。

### 2. 測試環境
在進行大規模修改前，請務必執行 `scratch/` 目錄下的測試腳本：
*   `test-logic.js`：測試純邏輯運算。
*   `test-backend.js`：測試 API 路由與權限。

---

## 🚀 部署新版本
每次修改 `Backend.gs` 後，必須：
1. 點選 **部署** -> **管理部署**。
2. 點選 **編輯 (鉛筆圖示)**。
3. 選取版本：**新版本**。
4. 點選 **部署**。
5. 確保網頁端對應的後端 URL 保持一致。
